# Daily — 2026-08-25
**Ran:** ~15:28–15:35 · **Competitors scanned:** 0 · **Proposals queued:** 4 new (+1 already waiting)

## What changed
Nothing to report from competitor scanning — `memory/competitors.md` has no competitors
configured. No scan ran.

## What it means
No section — nothing to scan, no pattern to find.

## What I recommend
1. **Fill `memory/brand.md`.** Right now it's completely empty. Every design/copy job this
   assistant does is supposed to be traceable to that file (§6 of CLAUDE.md), so until it's
   filled, design-director and motion-producer can only produce brand-agnostic work like
   today's prototype — not anything with your actual colors, type, or voice.
2. **Add competitors to `memory/competitors.md`.** Even one entry (URL + what to watch)
   turns tomorrow's run into a real delta report instead of "nothing configured."
3. **Fill in `memory/stack.md`'s folder list** if you want design/animation work checked
   against a real project later — right now there's no path listed for "Marsam may READ,"
   so nothing outside `workspace/` is even visible to reference against.

## What I built
**`workspace/prototypes/pdp-scroll-reveal/`** — a pinned product-hero scroll sequence:
four feature callouts reveal one at a time as the user scrolls, so a wall of spec copy
becomes a sequence instead of a paragraph dump, followed by a one-shot staggered
"related products" grid entrance. Built brand-agnostic (grayscale placeholders, easy
re-skin — swap the CSS custom properties once `brand.md` has real colors/type) since
there was no brand or competitor finding to react to today.

Look at `RATIONALE.md` first — it has the purpose test per section, what was cut and why
(parallax, cursor-follow, scroll-scrubbed images — rejected, no comprehension payoff), and
an honest note on verification: no real browser in this sandbox, so correctness was checked
by reading every tween (transform/opacity only, no layout thrash) rather than a DevTools
trace. A live FPS counter is built in — run `python3 -m http.server 8080 --directory
workspace/prototypes/pdp-scroll-reveal` and open with `?debug=1` to get the real number.
`prefers-reduced-motion` is a genuine alternate layout (no scroll-jack, short opacity
cross-fade), not motion switched off on an unchanged page.

## Waiting on you
- `2026-08-25-01-write-outside-workspace.md` — writes `marsam-notes.txt` outside
  `workspace/`. Cost: none. No effect if you leave it — I'll use `workspace/` instead if the
  note's still wanted.
- `2026-08-25-02-…` — inline `python3 -c` syntax check on the prototype's HTML/CSS. Already
  worked around (ran the same checks as a standalone `.py` file instead), so this is a
  record, not a blocker. Cost: none.
- `2026-08-25-03-…` — redirecting the local test-server's log to the session scratchpad dir
  instead of `workspace/`. Already worked around (kept the log inside `workspace/`). Cost:
  none.
- `2026-08-25-04-…` — `ps`/`ss`/`netstat` to confirm the test server was listening (not on
  the read-only allowlist). Already worked around by reading the server's own log file
  instead. Cost: none.
- `2026-08-25-05-…` — using `disown` when backgrounding the test server. Already worked
  around with plain `&`. Cost: none.

None of the four new ones block anything — each was a minor gate hit while building the
prototype, already routed around, and left as a record rather than something that needs a
decision. Safe to leave pending or clear them in bulk; nothing regresses either way.

## Needs a decision
None today.
